apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argo
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: http
  hostnames: ["argo.ad.pospiech.dev"]
  rules:
    - backendRefs:
        - name: argocd-server
          port: 80
