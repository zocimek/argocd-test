apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argo
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network
  hostnames: ["argo.ad.pospiech.dev"]
  rules:
    - backendRefs:
        - name: argo-cd-server
          port: 80
